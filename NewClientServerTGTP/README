===// Client to the TGTP server //===

*) Compile

$> make

It requires the BOOST library (property_tree)

*) No instalation required

*) Use (the "hilbert.mat.uc.pt" is the TGTP server) 

$> client hilbert.mat.uc.pt 2093 <query>

where <query> can be:

**) a simple word (e.g. ceva)

**) a simple (one line only) string (e.g. 'ceva theorem', notice the '')

**) a query and filters (e.g. 'ceva AND drawerId=4')

for more information about this last option contact the autors

*) More information

See the code "client.cpp" for more information about the information
received from the server. 


Pedro Quaresma (pedro@mat.uc.pt)